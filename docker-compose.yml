services:
  ipxa:
    image: liberatti/ipxa:latest
#    environment:
#      - IBLOCKLIST_USERNAME=
#      - IBLOCKLIST_PASSWORD=
#      - MAXMIND_ACCOUNT_ID=
#      - MAXMIND_LICENSE_KEY=
    volumes:
      - data:/data
    ports:
      - "5000:5000"
    deploy:
      resources:
        limits:
          memory: 256M
volumes:
  data: