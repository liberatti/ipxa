volumes:
  data:

services:
  web:
    image: node:lts
    working_dir: /app
    command: sh -c "npm install --legacy-peer-deps && npm run start "
    ports:
      - "4200:4200"
    volumes:
      - "./web:/app" 
  api:
    build: .
    image: liberatti/ipxa:latest
    environment:
      - API_KEY=dev
      #      - SECURITY_ENABLED=false
      #      - IPINFO_TOKEN=
      #      - IBLOCKLIST_USERNAME=
      #      - IBLOCKLIST_PASSWORD=
      #      - MAXMIND_ACCOUNT_ID=
      #      - MAXMIND_LICENSE_KEY=
    volumes:
      - data:/data
      - ./hooks:/opt/ipxa/hooks
      - ./api:/opt/ipxa/api
      - ./config:/opt/ipxa/config
      - ./config.py:/opt/ipxa/config.py
      - ./main.py:/opt/ipxa/main.py
            
    ports:
      - "5000:5000"
    deploy:
      resources:
        limits:
          memory: 256M